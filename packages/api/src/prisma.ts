import { PrismaClient } from '../generated/prisma';

const prisma = new PrismaClient();

// This is pretty useful when you want to write some code and debug at the same time which is pretty cool to me idk

export { prisma };
